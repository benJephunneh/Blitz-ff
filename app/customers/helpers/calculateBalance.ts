import { Location } from "@prisma/client"

const calculateBalance = (locations: Location[]) => {
  let amountOwed = 0
  let amountPaid = 0
  let balance = 0

  return {
    locations: locations,
  }
}

export default calculateBalance
