import { useMemo } from "react"
import calculateBalance from "../helpers/calculateBalance"

type UseLocationsProps = {
  locations: number
}
const useCalculateBalanceSheet = (locations: UseLocationsProps) => {
  return 42
}

export default useCalculateBalanceSheet
